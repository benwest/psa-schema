{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "psa/exhibitions.json",
    "type": "object",
    "properties": {
        "items": {
            "type": "array",
            "items": {
                "$ref": "definitions/exhibition-thumbnail.json"
            }
        },
        "info": {
            "$ref": "definitions/pagination.json"
        }
    },
    "required": [
        "items",
        "info"
    ],
    "additionalProperties": false
}